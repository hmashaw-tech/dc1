{
  "name": "ninjaapp",
  "version": "1.0.0",
  "description": "ninjaApp",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "NODE_ENV=test MOCHA_FILE=./jenkins-test-results.xml nodemon --exec 'mocha --recursive -R mocha-junit-reporter'",
    "test-git": "NODE_ENV=test MOCHA_FILE=./jenkins-test-results.xml mocha --recursive -R mocha-junit-reporter"
  },
  "author": "Mark",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "mocha": "^5.0.0",
    "mongoose": "^5.0.4",
    "supertest": "^3.0.0"
  },
  "devDependencies": {
    "mocha-junit-reporter": "^1.17.0",
    "nodemon": "^1.17.1"
  }
}
